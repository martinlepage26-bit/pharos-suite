# PHAROS Preview Backend Runbook

Last updated: 2026-03-30

## Status

This runbook is retired.

The former preview stack it described no longer exists as a live supported path:

- `pharos-suite-review.pages.dev` was deleted with the preview Pages project
- `preview-api.pharos-ai.ca` was retired with the old preview tunnel/backend path

## Current operator rule

- Do not use this file as a live setup guide.
- Do not treat the deleted preview hostnames as current verification targets.
- If a PHAROS preview backend must be restored, create a new preview architecture first, then replace this retired runbook with fresh live-verified instructions.

## Historical scope

This file remains only as a marker that a preview-only backend path once existed. It is not the source of truth for the current PHAROS deployment.
