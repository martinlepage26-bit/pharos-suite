#!/usr/bin/env bash
set -euo pipefail

cat <<'EOF'
PHAROS preview frontend deployment is retired as of 2026-03-30.

The former Cloudflare Pages project `pharos-suite-review` was deleted during the two-surface consolidation.
If a preview surface is needed again, create and document a new preview project before reintroducing this helper.
EOF

exit 1
