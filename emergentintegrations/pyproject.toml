[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "emergentintegrations"
version = "0.1.0"
description = "Local vendored shim for emergentintegrations (monorepo)."
requires-python = ">=3.9"
dependencies = ["requests>=2.31.0"]
