[tool.pytest.ini_options]
markers = [
    "unit: mock-only tests — no live services required (default for CI)",
    "integration: tests requiring live services or a running server — excluded from CI by default",
]

[tool.bandit]
skips = ["B101"]
